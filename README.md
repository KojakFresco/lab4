# Лабораторная №4. Симуляция Казино и Гусей

## Описание
Данный проект представляет собой симуляцию казино, где участвуют игроки, гуси и денежные потоки. Симуляция включает в себя случайные события, такие как ставки игроков, действия гусей и вращение колеса казино. Все события логируются в консоль с использованием цветного форматирования для удобства восприятия.

## Основные возможности

### 1. Игроки
- Игроки могут делать ставки на красное, чёрное или зеро.
- Баланс игроков изменяется в зависимости от исхода ставок.
- Игроки добавляются в казино с начальным балансом, который определяется случайным образом с учётом вероятностей.

### 2. Гуси
- Гуси представлены двумя типами:
  - **HonkGoose**: издаёт громкий крик, который влияет на симуляцию.
  - **RichGoose**: раздаёт деньги игрокам.
- Гуси добавляются в казино с равномерным распределением между типами.

### 3. Ставки
- Игроки могут делать ставки на:
  - Красное
  - Чёрное
  - Зеро
- Ставки логируются, и их результаты отображаются в консоли.

### 4. Коллекции
- Пользовательские коллекции для игроков, гусей и ставок:
  - **PlayerCollection**: поддерживает индексацию, срезы и итерацию.
  - **GooseCollection**: аналогично поддерживает индексацию, срезы и итерацию.
  - **CasinoBets**: словарная коллекция для хранения ставок игроков.

### 5. События
На каждом шаге симуляции происходит одно случайное событие:
- Игрок делает ставку.
- Вращение колеса казино.
- Гусь крадёт деньги у игрока.
- Гусь выполняет своё уникальное действие.
- Добавление нового игрока или гуся в казино.

### 6. Логирование
- Все события симуляции логируются в консоль с использованием цветного форматирования и эмодзи.
- Логи записываются в файл `sim.log` для последующего анализа.

### 7. Умный расчёт весов вероятностей
- В симуляции используется динамическая система расчёта весов для случайных событий.
- Вероятности событий адаптируются в зависимости от текущего состояния казино:
  - Если игроков слишком много, увеличивается вероятность добавления гуся.
  - Если ставок мало, увеличивается вероятность того, что игрок сделает ставку.
  - Если баланс игроков высокий, гуси чаще крадут деньги.
- Это позволяет симуляции быть более реалистичной и сбалансированной.

## Запуск

### Установка
1. Убедитесь, что у вас установлен Python 3.10 или выше.
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

### Запуск симуляции
Для запуска симуляции используйте следующую команду:
```bash
python -m src.main run-simulation --steps <количество_шагов> --seed <число_для_генерации>
```
Пример:
```bash
python -m src.main run-simulation --steps 20 --seed 42
```

### Тестирование
Для запуска тестов выполните:
```bash
pytest
```

## Структура проекта
- `src/` - исходный код проекта.
  - `casino.py` - основной класс казино.
  - `player.py` - класс игрока и коллекция игроков.
  - `goose.py` - классы гусей и их коллекция.
  - `chip.py` - класс фишек казино.
  - `simulation.py` - функция запуска симуляции.
- `tests/` - тесты для проверки функциональности.
- `sim.log` - файл логов симуляции.

## Пример работы
Пример вывода в консоль:
```
🎰 Игрок Алекс сделал ставку: 100 на красное. Баланс после ставки: 200
🎡 Колёсико вращается...
🎲 Выпало: 7 (красное)
💰 Игрок Алекс поставил 100 на красное и ВЫИГРАЛ. Новый баланс: 400
```
